# Database Configuration
# Connection pooling params: connection_limit (max connections), pool_timeout (seconds)
DATABASE_URL=postgresql://inventaire:inventaire_pwd@localhost:5432/inventaire?connection_limit=10&pool_timeout=30

# JWT Secrets
# CRITICAL: Generate NEW random secrets for production using one of these commands:
#   Node.js: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
#   OpenSSL: openssl rand -base64 64
#   PowerShell: [Convert]::ToBase64String((1..64 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
#
# Example secrets below (DO NOT use in production - generate your own!):
JWT_ACCESS_SECRET=PLACEHOLDER_GENERATE_YOUR_OWN_SECRET_USING_COMMAND_ABOVE_64_BYTES_MINIMUM
JWT_REFRESH_SECRET=PLACEHOLDER_GENERATE_YOUR_OWN_SECRET_USING_COMMAND_ABOVE_64_BYTES_MINIMUM

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# File Upload
SIGNATURES_DIR=./uploads/signatures

# Server Configuration
PORT=3001
NODE_ENV=development

# Sentry Error Tracking
# Get your DSN from https://sentry.io/ after creating a project
# Leave empty to disable Sentry (development)
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
SENTRY_TRACES_SAMPLE_RATE=1.0

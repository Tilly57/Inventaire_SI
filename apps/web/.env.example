# API Configuration
# URL de l'API backend (sans slash final)
VITE_API_URL=http://localhost:3001/api

# Environment
# Development: 'development' | Production: 'production'
VITE_ENV=development

# Sentry Error Tracking (Frontend)
# Get your DSN from https://sentry.io/ after creating a project
# Leave empty to disable Sentry (development)
VITE_SENTRY_DSN=https://YOUR_FRONTEND_DSN_HERE@o00000000.ingest.sentry.io/0000000
VITE_SENTRY_ENVIRONMENT=development

# Sentry Performance Monitoring
# Sample rate for performance traces (0.0 to 1.0)
# 1.0 = 100% of transactions tracked
# Production recommendation: 0.1 to 0.3 (to control quota usage)
VITE_SENTRY_TRACES_SAMPLE_RATE=1.0

# Sentry Session Replay
# Sample rate for normal session replays (0.0 to 1.0)
# Production recommendation: 0.1 (10% of sessions)
VITE_SENTRY_REPLAYS_SESSION_SAMPLE_RATE=0.1

# Sample rate for error session replays (0.0 to 1.0)
# Production recommendation: 1.0 (100% of error sessions)
VITE_SENTRY_REPLAYS_ON_ERROR_SAMPLE_RATE=1.0

# Application version (for release tracking in Sentry)
VITE_APP_VERSION=0.9.7

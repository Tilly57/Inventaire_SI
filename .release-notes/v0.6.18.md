# Release v0.6.18 - 2026-01-05

**Type:** Hotfix
**Branch:** release/0.6.18

## 🎯 Objectif

Correction urgente de l'erreur Docker build dans le CI/CD pipeline qui empêchait la construction de l'image web.

## 🐛 Corrections de bugs

### Docker Build Error (Critique)

**Problème:** Le CI/CD échouait lors du build Docker de l'application web avec l'erreur :
```
ERROR: failed to build: failed to solve:
process "/bin/sh -c npm ci --only=production=false"
did not complete successfully: exit code: 1
```

**Cause:** Syntaxe npm incorrecte dans le Dockerfile web (ligne 10)

**Avant:**
```dockerfile
RUN npm ci --only=production=false
```

**Après:**
```dockerfile
RUN npm ci
```

**Explication:**
- La syntaxe `--only=production=false` est invalide
- La syntaxe correcte est `--production=false` ou simplement `npm ci`
- Pour le build stage, on a besoin de toutes les dépendances (y compris devDependencies)
- `npm ci` installe tout par défaut, ce qui est correct pour le builder stage

## 🔧 Fichiers Modifiés

- `apps/web/Dockerfile` (ligne 10) : Correction syntaxe npm ci

## 📊 Impact

**Avant v0.6.18:**
- ❌ CI/CD docker-build job échouait systématiquement
- ❌ Impossible de construire l'image Docker web
- ❌ Pipeline CI bloqué

**Après v0.6.18:**
- ✅ Docker build web fonctionne correctement
- ✅ CI/CD pipeline complet opérationnel
- ✅ Images Docker construites avec succès

## 📝 Commits

- `d8eeb58` fix(docker): Correct npm ci syntax in web Dockerfile

## 🔗 Contexte

Ce fix fait suite au déploiement v0.6.17 qui a activé le docker-build sur les branches main et staging. L'erreur n'était pas visible auparavant car le job docker-build n'était exécuté que sur les pull requests.

---

**Testé par:** Claude Code
**Type:** Hotfix critique CI/CD
**Urgence:** Haute (bloque pipeline)
